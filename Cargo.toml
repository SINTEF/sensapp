[package]
name = "sensapp"
version = "0.3.0"
edition = "2024"

[features]
default = ["postgres", "sqlite"]
# Storage backends
postgres = ["sqlx/postgres"]
sqlite = ["sqlx/sqlite", "sqlx/regexp"]
timescaledb = ["sqlx/postgres"] # Uses same driver as postgres
duckdb = ["dep:duckdb"]
bigquery = [
  "dep:gcp-bigquery-client",
  "dep:bigdecimal",
  "dep:big-decimal-byte-string-encoder",
  "dep:tonic",
]
rrdcached = ["dep:rrdcached-client"]
clickhouse = ["dep:clickhouse"]
# Convenience feature to enable all storage backends
all-storage = [
  "postgres",
  "sqlite",
  "timescaledb",
  "duckdb",
  "bigquery",
  "rrdcached",
  "clickhouse",
]
# Test utilities
test-utils = []

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
arrow = { version = "58", features = ["ipc", "ipc_compression"] }
arrow-ipc = { version = "58", features = ["lz4"] }
arrow-schema = "58"
arrow-array = "58"
anyhow = "1.0"
async-trait = "0.1"
thiserror = "2.0"
axum = { version = "0.8", features = ["multipart", "macros"] }
base64 = "0.22"
futures = "0.3"
sqlx = { version = "0.8", features = [
  "runtime-tokio",
  "uuid",
  "json",
  "time",
  "rust_decimal",
] }
tokio = { version = "1.48", features = ["full"] }
tokio-stream = { version = "0.1", features = ["io-util"] }
tokio-util = "0.7"
tower = { version = "0.5", features = ["full"] }
tower-http = { version = "0.6", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
uuid = { version = "1.18", features = ["serde"] }
csv-async = "1.3"
rust_decimal = "1.39"
geo = "0.32"
cached = { version = "0.59", features = ["async", "tokio", "async-trait"] }
nom = "8.0"
sindit-senml = "0.3"
serde_json = "1.0"
hifitime = "4.2"
iso8601 = "0.6"
duckdb = { version = "1.3", features = ["bundled"], optional = true }
serde = "1.0"
confique = { version = "0.4", features = ["toml"] }
byte-unit = "5.1"
prost = "0.14"
snap = "1.1"
rusty-chunkenc = "0.1"
blake3 = "1.8"
regex = "1.12"
influxdb-line-protocol = "2.0"
flate2 = "1.1.4"
smallvec = "1.15"
once_cell = "1.21.4"
urlencoding = "2.1"
hybridmap = "0.1.3"
sentry = { version = "0.47", features = ["anyhow", "tower"] }
url = "2.5"
gcp-bigquery-client = { version = "0.28", optional = true }
sinteflake = { version = "0.1", features = ["async"] }
tonic = { version = "0.14", optional = true }
bigdecimal = { version = "0.4", optional = true }
big-decimal-byte-string-encoder = { version = "0.1", optional = true }
clru = "0.6"
utoipa = { version = "5.4", features = ["axum_extras"] }
utoipa-scalar = { version = "0.3", features = ["axum"] }
rrdcached-client = { version = "0.2", optional = true }
clickhouse = { version = "0.15", features = [
  "lz4",
  "uuid",
  "time",
  "inserter",
], optional = true }
jsonwebtoken = { version = "10", features = ["rust_crypto"] }
rustls = "0.23"
rusty-promql-parser = "0.2.1"
prometheus-client = "0.24"
simplify-polyline = "0.5"

[dev-dependencies]
temp-env = "0.3"
tokio-test = "0.4"
serial_test = "3.1"
jsonwebtoken = { version = "10", features = ["rust_crypto"] }
# Re-import the same crate with test-utils feature for integration tests
sensapp = { path = ".", features = ["test-utils"], default-features = false }
